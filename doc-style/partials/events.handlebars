<div id="event_{{name}}" class="panel panel-code">
  <div class="panel-heading">
    <h5>
      {{name}}

      {{#if since}}
        <span class="badge">Version {{since}}</span>
      {{/if}}
    </h5>
    {{#if overwritten_from}}
    <p>
      Inherited from
      <a href="{{crossLinkRaw overwritten_from/class}}#method_{{overwritten_from/name}}">{{overwritten_from/class}}</a>
      {{#if foundAt}} but overwritten in {{/if}}
    </p>
    {{else}} {{#if extended_from}}
    <p>
      Inherited from:
      <a href="{{crossLinkRaw extended_from}}#method_{{name}}">{{extended_from}}</a>
    </p>
    {{else}} {{#providedBy}}
    <p>Provided by the <a href="../modules/{{.}}.html">{{.}}</a> module.</p>
    {{/providedBy}} {{/if}} {{/if}} {{#if providedBy}} {{#providedBy}}
    <p>Provided by the <a href="../modules/{{.}}.html">{{.}}</a> module.</p>
    {{/providedBy}} {{/if}} {{#if foundAt}}
    <p>
      Defined in: <a href="{{foundAt}}">`{{{file}}}:{{{line}}}`</a>
    </p>
    {{/if}}
  </div>

  <div class="panel-body">
    <blockquote>
      {{#if default}}
        <div class="code-item-section">
          <h4>Default value:</h4>
          <code class="prettyprint">{{default}}</code>
        </div>
      {{/if}}
  
      {{#if deprecated}}
      <p>
        <em class="glyphicon glyphicon-alert" aria-hidden="true"></em> This method is deprecated. Please refrain from using this as this would be unsupported in future releases. {{#if deprecationMessage}}
        <br>
        <br><small>{{deprecationMessage}}</small>
        {{/if}}
      </p>
      {{/if}} {{#if access}}
      <p>
        <em class="glyphicon glyphicon-eye-close" aria-hidden="true"></em> Access level: {{{access}}}
      </p>
      {{/if}} {{#if final}}
      <p>
        <em class="glyphicon glyphicon-lock" aria-hidden="true"></em> This method cannot be overwritten.
      </p>
      {{/if}} {{#if multiple}}
      <span class="label label-warning" title="This argument may occur one or more times.">multiple</span>
      {{/if}} {{#if required}}
      <span class="label label-warning" title="This attribute is required for communication.">required</span>
      {{/if}} {{#if overwritten_from}}
      <p>
        <em class="glyphicon glyphicon-edit" aria-hidden="true"></em> This method overwrites inherited class method.
      </p>
      {{/if}} {{#if static}}
      <p>
        <em class="glyphicon glyphicon-arrow-left" aria-hidden="true"></em> This method is a static method
      </p>
      {{/if}} {{#if chainable}}
      <p>
        <em class="glyphicon glyphicon-refresh" aria-hidden="true"></em> This method is chainable.
      </p>
      {{/if}} {{#if readonly}}
      <span class="label label-info" title="This attribute is reference only.">readonly</span>
      {{/if}} {{#if async}}
      <p>
        <em class="glyphicon glyphicon-random" aria-hidden="true"></em> This method invokes asycronously.
      </p>
      {{/if}}
    </blockquote>
    <p>{{{eventDescription}}}</p>
  </div>
  <div class="panel-body">
    <div class="col-md-6">
      {{#if params}}
      <div class="code-section-item">
        <h6>Event Payload:</h6>
        <ul class="params-list">
          {{#params}} {{>param}} {{/params}}
        </ul>
      </div>
      {{/if}}
    </div>


    <div class="col-md-6">
      <pre class="prettyprint">
      // Example for constructor {{name}} ()
      {{#example}}
        {{{.}}}
      {{/example}}
    </pre>
    </div>
  </div>
</div>

<!--

  {{#if emit}}
    <div class="code-item-section">
      <h4>Fires event <code>{{name}}Change</code></h4>
      <p>
      Fires when the value for the configuration attribute `{{{name}}}` is
      changed. You can listen for the event using the `on` method if you
      wish to be notified before the attribute's value has changed, or
      using the `after` method if you wish to be notified after the
      attribute's value has changed.
      </p>
      <div class="params">
        <h4>Parameters:</h4>
        <ul class="params-list">
          <li class="param">
            <code class="param-name">e</code>
            <span class="type">{{#crossLink "EventFacade"}}{{/crossLink}}</span>

            <div class="param-description">
              An Event Facade object with the following
              attribute-specific properties added:
            </div>

            <ul class="params-list">
              <li class="param">
                <code class="param-name">prevVal</code>
                <span class="type">Any</span>
                <div class="param-description">The value of the attribute, prior to it being set.</div>
              </li>
              <li class="param">
                <code class="param-name">newVal</code>
                <span class="type">Any</span>
                <div class="param-description">The value the attribute is to be set to.</div>
              </li>
              <li class="param">
                <code class="param-name">attrName</code>
                <span class="type">{{#crossLink "String"}}{{/crossLink}}</span>
                <div class="param-description">The name of the attribute being set.</div>
              </li>
              <li class="param">
                <code class="param-name">subAttrName</code>
                <span class="type">{{#crossLink "String"}}{{/crossLink}}</span>
                <div class="param-description">If setting a property within the attribute's value, the name of the sub-attribute property being set.</div>
              </li>
            </ul>
          </li>
        </ul>
      </div>
    </div>
  {{/if}}
-->
<!--
<div id="event_{{name}}" class="code-item attr item {{#if access}}{{access}}-{{/if}}event-item {{#if deprecated}} deprecated{{/if}}{{#if extended_from}} inherited{{/if}}">
  <h3 class="code-item-name"><code>{{name}}</code></h3>

  <div class="code-item-properties">
    <div class="properties-wrapper">
      {{#if deprecated}}
        <span class="label label-warning" title="This property is deprecated. Might be removed in future releases.">deprecated</span>
      {{/if}}

      {{#if unsupported}}
        <span class="label label-warning" title="This feature may not be supported in the specs. Kept for future releases.">unsupported</span>
      {{/if}}

      {{#if access}}
        <span class="label label-default" title="This event is {{access}}.">{{access}}</span>
      {{/if}}

      {{#if final}}
        <span class="label label-danger" title="This event is final and cannot be changed.">final</span>
      {{/if}}

      {{#if development}}
        <span class="label label-danger label-development" title="This feature is still in development. In may not be available in the current release.">in-development</span>
      {{/if}}

      {{#if overwritten_from}}
        <span class="label label-danger" title="This event is overwritten.">overwritten</span>
      {{/if}}

      {{#if static}}
        <span class="label label-default" title="This event is static.">static</span>
      {{/if}}

      {{#if global}}
        <span class="label label-info label-global" title="This event is placed in the global class namespace.">global</span>
      {{/if}}
    </div>

    <div class="code-item-information">
      {{#if overwritten_from}}
        <span>
          Inherited from
          <a href="{{crossLinkRaw overwritten_from/class}}#attr_{{overwritten_from/name}}">{{overwritten_from/class}}</a>
          {{#if foundAt}}
            but overwritten in
          {{/if}}
        </span>
      {{else}}
        {{#if extended_from}}
          <span>
            Inherited from:
            <a href="{{crossLinkRaw extended_from}}#attr_{{name}}">{{extended_from}}</a>
          </span>
        {{else}}
          {{#providedBy}}
            <span>Provided by the <a href="../modules/{{.}}.html">{{.}}</a> module.</span>
          {{/providedBy}}
        {{/if}}
      {{/if}}
      {{#if deprecationMessage}}
        <span>Deprecated: {{deprecationMessage}}</span>
      {{/if}}
      {{#if providedBy}}
        {{#providedBy}}
          <span>Provided by the <a href="../modules/{{.}}.html">{{.}}</a> module.</span>
        {{/providedBy}}
      {{/if}}
    </div>

    {{#if foundAt}}
      <small>
        <i class="fa fa-chevron-right"></i>&nbsp;&nbsp;Defined in: <a href="{{foundAt}}">`{{{file}}}:{{{line}}}`</a>
      </small>
    {{/if}}

    {{#if since}}
      <small><i class="fa">&nbsp;&nbsp;&nbsp;</i>&nbsp;&nbsp;Available since <span class="badge">{{since}}</span></small>
    {{/if}}
  </div>

  <div class="code-item-description">
    {{{eventDescription}}}
  </div>

  {{#if params}}
    <div class="code-item-section">
      <h4>Event Payload:</h4>

      <ul class="params-list">
      {{#params}}
          <li class="param">
            <code class="param-name">{{name}}</code>
            <span class="type"><b>{</b>{{#crossLink type}}{{/crossLink}}<b>}</b></span>

            {{#if optional}}<span class="label">Optional</span>{{/if}}
            {{#if multiple}}<span class="label multiple" title="This argument may occur one or more times.">Multiple</span>{{/if}}

            <div class="param-description">
                {{{description}}}
            </div>

            {{#if props}}
              <ul class="params-list">
                {{#props}}
                  <li class="param">
                    <code class="param-name">{{name}}</code>
                    <span class="type"><b>{</b>{{#crossLink type}}{{/crossLink}}<b>}</b></span>

                    {{#if optional}}<span class="label">Optional</span>{{/if}}
                    {{#if multiple}}<span class="label multiple" title="This argument may occur one or more times.">Multiple</span>{{/if}}

                    <div class="param-description">
                      {{{description}}}
                    </div>

                    {{#if props}}
                      <ul class="params-list">
                        {{#props}}
                          <li class="param">
                            <code class="param-name">{{name}}</code>
                            <span class="type"><b>{</b>{{#crossLink type}}{{/crossLink}}<b>}</b></span>

                            {{#if optional}}<span class="label">Optional</span>{{/if}}
                            {{#if multiple}}<span class="label multiple" title="This argument may occur one or more times.">Multiple</span>{{/if}}

                            <div class="param-description">
                              {{{description}}}
                            </div>
                          </li>
                        {{/props}}
                      </ul>
                    {{/if}}
                  </li>
                {{/props}}
              </ul>
            {{/if}}
          </li>
      {{/params}}
      </ul>
    </div>
  {{/if}}

  {{#return}}
    <div class="code-item-section">
      <h4>Returns:</h4>

      <div class="returns-description">
        {{#if description}}
          {{#if type}}
            <span class="type"><b>{</b>{{#crossLink type}}{{/crossLink}}<b>}</b></span>:
          {{/if}}
          {{{description}}}
        {{else}}
          {{#if type}}
            <span class="type"><b>{</b>{{#crossLink type}}{{/crossLink}}<b>}</b></span>:
          {{/if}}
        {{/if}}
      </div>
    </div>
  {{/return}}

  {{#example}}
    <div class="code-item-section">
      <h4>Example:</h4>
      <pre class="example-content prettyprint">
        {{{.}}}
      </pre>
    </div>
  {{/example}}
  <hr>
</div>-->